import math
angle_deg = float(input("Enter angle in degrees"))
angle_rad = math.radians(angle_deg)

rad_sin = math.sin(angle_rad)
deg_sin = math.sin(angle_deg)

rad_tan = math.tan(angle_rad)
deg_tan = math.tan(angle_deg)

rad_cos = math.cos(angle_rad)
deg_cos = math.cos(angle_deg)

rad_cot = math.cot(angle_rad)
deg_cot = math.cot(angle_deg)